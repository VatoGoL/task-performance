В Данном файле задокументированы мои попытки оптимизации сайта

-index.html: скрипт stat.js теперь загружается асинхронно (добавлен атрибут async)
-contacts.js: Изменена функция addContacts, теперь она загружает лишь часть контактов,
              а не все сразу. При скролле догружаются новые
-contacts.js: Добавлена константа contactHeight равная 18 - высота каждого контакта
-contacts.js: Заменена логика расчета topItemIndex, с перебора всех жэлементов, на деление на константу. Т.к. высота всех элементов одинакова
-contacts.js: Строчка "stickyHeader.textContent = items[topItemIndex].textContent;", заменена на "stickyHeader.textContent = topItemIndex;". За ненадобностью обращения к массиву, т.к. индекс равен содержимому
-contacts.js: Удалён массив "itemOffsets", за ненадобностью
-contacts.js: "items" был превращён из массива в переменную хранящую количество элементов класса "contact".